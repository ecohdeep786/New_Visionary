# STATUS — internal product, append-only

## [Wave 0.5] [01-product-manager — entry reconciliation] — 2026-09-19

Done: Read the complete v2.2 contract, legacy MASTER_SPEC, packed charters, decisions and QA. No internal STATUS or agents directory existed at discovery. Begin missing DESIGN_INVENTORY under A8; preserve the existing implementation rather than restarting previous work. This entry is administrative and does not complete the PM handoff.

Files: Consolidated the internal contract and existing DECISIONS/QA logs into docs/visionary; preserved superseded pack files in archive/legacy-pack; created entry points and PM charter. The separate Frontend(Head Of Product Agent) folder was not opened or edited.

Decisions: v2.2 alone is operative. Previous conversation-first implementation remains current-build evidence, not a requirement to rebuild or discard. New contract differences become scoped acceptance gaps. No app source edits authorized in this PM stage.

Open for next: PM inventory, student role pod and remaining role acceptance addenda in progress. Do not start UX yet. No previous completed wave or gate pass was found in the supplied internal logs.

QA notes: Historical QA and route existence are not fresh acceptance. Existing unrelated working-tree changes belong to the user/other team and remain untouched. No merge or wave-completion verdict.

## [Wave 0.5] [01-product-manager — Student role pod] — 2026-09-19

Done: Read v2.2 fully, the archived MASTER_SPEC and packed PM charter, protocol, decisions and QA; re-read the canonical agents README/PM charter and last STATUS entry after their creation. Inspected current student routes, Home/Guide, Learn/objective, lesson activity, Practice, Build, Progress, Classes, domain/session/evidence services and persona seeds. Produced route-level student acceptance, stage-specific modules and next actions, state requirements, metric definitions, fixture criteria, Part W transition PRD and STU-01–09 traceability.

Files: docs/visionary/roles/student.md; this append-only handoff. No frontend/source changes. Protected Frontend documentation was not opened or edited.

Decisions: Keep existing shell, local learning/session/cube/classwork/artifact foundations; treat decision-led Home and stage adaptation as scoped enhancements under v2.2, not a rebuild. Persona labels do not establish stage adaptation. Current source has no stageProfile/auto-transition domain; G12 remains an implementation gap. Proposed postpone semantics and preservation of post-transition work during Undo are explicitly awaiting chief acceptance, not silently settled.

Open for next: Main PM consolidates the design inventory and other role addenda; student pod stops here. After user review, 02-UX should reconcile Home/Ask entry, canvas-first lesson flow and state/transition designs within current DNA. Fixture approval and design inventory co-sign require the stated evidence; no complete student fixture approval issued.

QA notes: Static source review only; no tests, browser checks, screenshots, accessibility certification or gate pass claimed by this pod. No source modified, so no product regression claim is inferred. Existing historical tests remain useful inputs for later 07-QA, not fresh certification.

## [Wave 0.5] [01-product-manager — consolidated handoff] — 2026-09-20

Done: Completed the product-side current-route/design inventory, five role acceptance addenda, shared state and integration requirements, and contract traceability. Preserved the existing internal implementation. Consolidated the loose internal pack into docs/visionary and archived eight superseded files without deleting their contents. Student PM pod completed its bounded charter; no later-stage agent ran.

Files: DESIGN_INVENTORY.md; roles/student.md, teacher.md, parent.md, professional.md, organization.md; SHARED_ACCEPTANCE.md; TRACEABILITY.md; canonical README/MASTER_SPEC/agents entry points; PRODUCT_GATE.md pointing to all twelve Part AA gates; archive/README.md and legacy-pack; baseline/wave-0.5 with four before screenshots and README; append-only DECISIONS, QA and this STATUS. No application source files changed by this stage. Protected Frontend(Head Of Product Agent) contents were not opened or modified.

Decisions: D-003–D-010. Enhance current blue/white shell and working domain/session/classwork foundations. Implement role/stage decision Homes and guided Ask under v2.2 rather than preserving superseded conversation-first acceptance. Automatic transitions follow Part W; edge semantics for Postpone and post-transition work during Undo remain explicitly proposed for 00 arbitration before implementation. Future backend cutover retires only identified test fixtures, preserving legitimate owned work. Voice, memory and sync remain capability-gated and user-controlled; no implemented-AGI or always-listening claim.

Open for next: **Await user review; next chain stage is 02-UX, still Wave 0.5.** Read this handoff, inventory, role addenda and shared acceptance. Define flow/state/component-use maps inside current design DNA; no frontend implementation yet. Complete missing organization and screen baselines when browser approval is available, reset the temporary viewport, and obtain 03's design-inheritance co-sign. 00 resolves the two transition edge semantics before 04 implements them. Do not restart existing work or treat persona labels as approved stage fixtures.

QA notes: Local Vite preview ran; four fictional role baseline PNGs saved at 1440×900. Organization capture stopped on browser approval-review usage-limit failure; no workaround used. Source/route inventory and product criteria are ready for review, but visual coverage is partial. No fresh automated suite, public pixel diff, accessibility certification or twelve-gate pass. **Wave 0.5 is not complete; merge/release is not authorized.** PM stops here as requested.

## [Wave 0.5] [02-ux-designer — Home/Ask enhancement slice] — 2026-09-20

Done: Read MASTER_SPEC, full operative v2.2, agents protocol, Part V charter, last STATUS, DECISIONS/QA, design inventory, shared acceptance and five role addenda. Inspected current DashboardHome, Guide, navigation, route aliases and workspace hook. Designed role-specific decision Home, evidence reasons, direct workflow entry, intent-first Ask, deep-link/session compatibility, responsive hierarchy and applicable state/accessibility behavior. Proceeded on chief's instruction reflecting the user's request to continue building without another review delay.

Files: UX_HOME_FLOW.md; this STATUS handoff; QA evidence note. Documentation only. No source, tokens, other agent deliverables or protected Frontend folders modified/opened.

Decisions: Applies D-003/D-007 inside current white/blue shell; no new product-policy amendment. Home and Ask get distinct jobs while existing Guide/session storage and activity URLs remain compatible. No fake stage adaptation or transition controls; unavailable data is not zero or inferred weakness. Primary operational actions open existing destinations without first sending chat.

Open for next: 03-DesignSystem co-signs component/token inheritance for this bounded Home/Ask slice, then04 implements the approved list. Full role completion, transition engine, stage fixtures and later Ask history features remain tracked gaps, not delivered by this handoff. Chief retains transition-edge arbitration. Next agent reads this entry;02 stops now.

QA notes: Static source/design review only; no runtime or automated checks by02 and no screenshots captured. Existing baseline coverage remains partial. No G1–G12 pass, wave completion or merge/release authorization asserted.

## [Wave 0.5] [03-design-system-engineer — Home/Ask inheritance] — 2026-09-20

Done: Read operative v2.2 fully, master/protocol/charter, last handoff, decisions/QA, UX flow and current inventory. Co-signed bounded Home/Ask inheritance; implemented additive scoped tokens/layout classes for priority, reasons, quiet rows and intent/context choices. Inspected current workspace stylesheet consumers. Existing global/shared selectors and public styles remain unchanged.

Files: src/components/dashboard/workspace.css; DESIGN_HOME_HANDOFF.md; append-only STATUS and QA. No other implementation, another agent's deliverable or protected Frontend folder changed.

Decisions: Apply D-003/D-007 through enhancement, not a new visual language. Retain existing contrast-safe blue controls and white shell; Part H token equivalents apply locally to new Home/Ask entry surfaces. Detailed keep/enhance/class API published for04. No new motion or dependency.

Open for next: 04 implements service-scoped Home and guided Ask with documented class API, preserving deep links, saved state and route compatibility. Capture before/after per enhanced screen; later05/06/07 verify live behavior. Full token normalization, stage adaptation, transition integrity and other role workflows are not delivered here.03 stops.

QA notes: PostCSS parsed stylesheet successfully (72 top-level nodes); scoped git diff whitespace check passed. Initial source-read paths were corrected to src/pages/dashboard without file changes. New classes are not yet consumed; no browser/visual/accessibility pass or full gate claim. Wave remains open.

## [Wave 0.5] [04-frontend-engineer — Home/Ask implementation] — 2026-09-20

Done: Continued03's approved enhancement list. The dispatched04 worker stopped on a usage limit before edits; the primary agent assumed the04 charter after re-reading contract/protocol/last handoff. Implemented five role decision Homes on a typed scoped service, direct actions and reasons, permission-safe parent summaries, workspace-keyed cancellable queries, guided Ask intent/material persistence, exact session-ID resume, compatible legacy entry routes, visible composer label, native-language doubt draft, direct operational links, safer deletion/error feedback and role Ask navigation. Preserved inherited CSS, lesson canvas, public source and stored records.

Files: homeService.ts, guideEntryService.ts, workspaceService.ts, domain/workspace.ts, useWorkspace.js, DashboardHome.jsx, DecisionHome.jsx, Guide.jsx, GuideEntry.jsx, dashboardNavigation.js, tests/home.test.mjs; after screenshots in baseline/wave-0.5; STATUS/QA/DECISIONS. Protected Frontend folder not opened. Unrelated .zcodeignore and shots left untouched.

Decisions: D-007 enhancement implemented; no layout-DNA replacement. Homes never invent learner weakness, queue counts or approved curriculum. No configured stageProfile is disclosed instead of inferred from persona names. Pasted material is stored, not claimed analyzed. Revision-keyed Home requests discard previously authorized summaries when permissions change. Full stage adaptation and Part W remain open.

Open for next: 05-Motion review of touched Home/Ask controls, then06 accessibility and07 formal QA. Keyboard tabs, focus return, full seven-width/200% zoom/long Indic sweep, parent connected-state browser run and public pixel comparison still need formal verification. Complete role workflow/transition gaps remain in TRACEABILITY. No gate pass or merge/release authorization.

QA notes: 30 Node tests pass (six added); lint/typecheck pass; production build passes with pre-existing ambiguous Tailwind easing warning. Runtime smoke: five roles on School demo account, Ask context/intent/question survive refresh, Home exact resume retains explaining stage, notes and Hindi, mobile390 activity-first and Hindi doubt composer focus. Before/after screenshots captured. Stale dev server import error resolved by restarting owned preview; no config/source workaround. These are04 smoke checks, not07 certification.04 stops.

## [Wave 0.5] [05-motion-engineer — Home/Ask review] — 2026-09-21

Done: Re-read master entry, full v2.2 and Part V05 charter, protocol, last04 handoff, decisions and QA after interruption. Reviewed DecisionHome, GuideEntry, Guide, inherited GuideActivity, scoped workspace CSS and shared dialog motion. New Home/Ask surfaces introduce no autoplay, shimmer, tween or decorative entrance animation. Stage and pane changes remain immediate; cube manipulation remains user-driven with a text alternative. No motion was added merely for polish.

Files: Append-only STATUS.md and QA.md. No application source, other agent deliverable, public source or protected Frontend folder modified.

Decisions: Preserve inherited restrained behavior. Scoped reduced-motion rules disable animation and transitions in the workspace; global near-zero-duration rules cover portal dialog animation. These are source findings, not runtime certification.

Open for next: 06-A11y verifies keyboard/focus, zoom and reduced-motion behavior;07 verifies the full runtime matrix. Existing shared-style issue M-01 requires chief routing to the shared-style owner: later html scroll-behavior:smooth overrides the earlier reduced-motion html auto declaration in src/index.css. This public/shared file is outside05's slice authority and was not changed. Do not claim G9 passed until resolved or bounded by verified runtime evidence.05 stops here.

QA notes: Static review only; current files rechecked on September21. The one requestAnimationFrame in Guide schedules composer focus, not visual animation. No browser reduced-motion emulation or fresh automated suite performed by05. Full wave and twelve-gate acceptance remain open.

## [Wave 0.5] [06-accessibility-engineer — Home/Ask semantics] — 2026-09-21

Done: Read the full operative contract, protocol, last05 handoff, decisions/QA and UX/design handoffs. Added roving keyboard tabs with panel relationships, dialog focus restoration (including deleted-row fallback), contextual delete names and inherited practical targets, separate material label/helper description, composer description, narrow response status announcements, teaching-draft language and localized saved-activity heading provenance. Mobile activity retains a route heading. No design rebuild.

Files: Guide.jsx, GuideEntry.jsx, DecisionHome.jsx, homeService.ts, tests/home.test.mjs; STATUS and QA. Chief explicitly authorized these semantic/focus edits to04 JSX and the optional titleLocale contract field. No03 CSS, shared/public source or protected Frontend folder touched.

Decisions: Preserve historical messages without guessing their language from the current preference; their domain lacks per-message locale. Current interface remains English, while known localized teaching text gets its language. Keep inherited dialog behavior and visual classes.

Open for next:07 runtime keyboard/focus, actual screen-reader and zoom/reduced-motion checks. Chief owns browser verification. M-01 shared smooth-scroll defect remains unchanged.04 owns material count wording: local editable characters must not be called saved after a storage failure. Historical message locale provenance remains an04 domain gap. Full waves and gates remain incomplete.06 stops here.

QA notes: Full30 Node tests, lint and both typechecks passed during patching; added language-provenance regression then all7 focused Home tests and final typecheck/scoped lint passed. Diff whitespace check passed. Final label/breakpoint-only refinements require07 final runtime/build check. No actual screen-reader,200% zoom, reduced-motion runtime or browser focus result claimed by06.

## [Wave 0.5] [07-qa-engineer — Home/Ask verification] — 2026-09-21

Done: Read master, full operative v2.2, protocol/PartV07, last06 handoff, decisions/QA, product gate and scoped UX/design contracts. Ran fresh31 tests, lint, both typechecks, production build and scoped whitespace check; inspected Home/Ask service/UI boundaries. Additional isolated service probes verified in-flight cancellation, permission recheck during pending parent Home load and stored conversation selection preservation after failed writes. Incorporated explicitly attributed coordinator browser checks.

Files: QA_HOME_RESULTS.md; append-only QA.md and STATUS.md. No application source, another agent deliverable, public source or protected Frontend folder touched. Unrelated scripts-tmp/shots work preserved.

Decisions: No new product decision. Passing automation is not full state or wave acceptance. Four04-owned failures need fixes and recheck: QA-H01 material count falsely says saved after failure; QA-H02 unsupported Ask fallback bypasses minor eligibility; QA-H03 historical message locale is missing; QA-H04 failed conversation-selection writes are not handled atomically in UI. M-01 remains owned by chief/shared-style team.

Open for next: Chief runs PartAA gate, routes named failures to04/shared-style owners, then07 reruns and chief rechecks. Finish pending browser focus/console/long-label checks and retain explicit unverified SR,200% zoom,reduced-motion/public pixel evidence. Broader stageProfile/PartW/role/connection gaps remain open. Merge/release must remain blocked;07 stops here.

QA notes:31/31 tests, lint,typecheck,build pass; pre-existing Tailwind easing warning retained. Coordinator reports no horizontal DOM overflow for Home/Ask at seven widths, parent Maya report routing, compact keyboard tab selection/focus and named history dialog. These are attributed limited checks, not a full browser/accessibility certification. No wave completion or G1–G12 pass claimed.

## [Wave 0.5] [00-head-of-product — first Home/Ask gate] — 2026-09-21

Done: Ran all twelve Part AA checks after07's handoff. App runs; bounded G1/G2/G10/G11 evidence passes. The gate as a whole is BLOCKED, not a full-wave completion.

Files: PRODUCT_GATE.md gate table; this handoff. No public/source changes.

Decisions: G3/G6 fail QA-H02 eligibility / owner04; G5 fails QA-H01 saved wording / owner04; G8 fails QA-H04 failed selection UI / owner04; G9 fails QA-H03 language provenance / owner04 then06 and M-01 / shared-style owner. G4 full lifecycle, G7 same-data visual regression and G12 Part W evidence are not passed. StageProfile and full role completion remain separate approved-slice work, not silently waived.

Open for next:04 corrects QA-H01–H04 with regression coverage and investigates composer hydration on exact session entry. Preserve06 semantics and03 styling; no redesign/public edits. Then06 language/focus recheck and07 rerun before chief re-gates. Chief independently completes live keyboard/focus evidence while04 works. Do not start another wave or release.

QA notes: Additional main browser check at360px: Escape from history restores Conversation history focus; cancelling the delete confirmation returns focus to the contextual Delete row, without deleting data. Browser recovery used the same local preview after restarting the owned dev server, with no security/settings workaround. Actual SR/zoom/reduced-motion/public pixel checks remain unverified.

## [Wave 0.5] [04-frontend-engineer — Home/Ask gate corrections] — 2026-09-22

Done: Read the operative contract fully, protocol, Part V04, last gate handoff, decisions, QA and QA_HOME_RESULTS. Continued the already-written partial corrective work instead of restarting it. Verified neutral material counts (QA-H01), centralized role/age eligibility for Guide fallback and matched responses (QA-H02), optional known-language block provenance without guessing legacy/user-text language (QA-H03), and persist-before-view History/New selection with handled errors (QA-H04). Completed single-snapshot route hydration so an exact session opens its own composer draft rather than another active conversation's draft. Successful material retry clears the prior error. Preserved06 focus/tab/helper behavior and03 styling.

Files: Completed Guide.jsx and guideEntryService.ts; added five regressions to tests/home.test.mjs; append-only STATUS and QA. Reviewed and retained the interrupted04 corrections already present in GuideEntry.jsx, journeyEligibility.ts, workspaceService.ts and domain/workspace.ts. No public source, shared CSS, protected Frontend documents, screenshots or unrelated work changed by this agent.

Decisions: Implements the gate's existing corrective authority and D-013; no new product policy. One route-entry result owns conversation, draft, canvas and pane together. Plain Ask entry still opens Conversation, while exact activity entry opens Activity. Optional block locale marks only known authored content; legacy records and arbitrary input remain unguessed.

Open for next:06 reviews the new language semantics and retained focus behavior;07 reruns the corrective acceptance and production build, including rendered storage-failure/retry when feasible; chief re-gates afterward. Main owns browser evidence and captures. StageProfile, Part W, complete connection/state matrix, M-01 and outstanding SR/zoom/reduced-motion/public-pixel evidence remain open. No next wave, merge, release or implemented AGI claim.04 stops here.

QA notes:36/36 Node tests pass, including five new regressions; both typechecks and lint pass; scoped whitespace check passes. Final focused12 Home tests also pass after preserving plain Ask's initial pane. Tests cover exact-session draft ownership, failed History/New selection preserving the complete prior view contract and stored records, retry, minor/unknown/non-learner fallback eligibility, mixed English/Hindi/Bengali block history, legacy language compatibility and material failed-save/retry. These are automated service/view-contract checks, not rendered failure or screen-reader certification. Coordinator independently reported minor fallback excludes the professional journey and separate fractions/unsupported-question drafts survive exact resume, reload and history switching; that browser evidence predates the final single-effect consolidation and requires final recheck.

## [Wave 0.5] [06-accessibility-engineer — corrective semantics review] — 2026-09-22

Done: Read master pointer, full operative v2.2 including Part V06, protocol, latest04 corrective handoff, decisions, QA, QA_HOME_RESULTS and PRODUCT_GATE. Source-reviewed Guide, GuideEntry, DecisionHome, response-block provenance, route/selection contracts and the inherited Radix dialog. Known authored text and activity/action labels render their own optional language; older and arbitrary user content are not relabelled from today's preference. History and delete errors now appear inside their active dialogs as alerts. The existing roving tabs, named panels, focus-return handlers, narrow response status and explicit material label/helper association remain intact. No new scoped semantic regression found that requires another source edit.

Files: Append-only STATUS.md and QA.md only. No application source, shared/public styles, another agent's implementation or protected Frontend documents changed. No browser operated by06.

Decisions: No product-policy change. This is a corrective source review, not a screen-reader or WCAG certification. Original05 static motion review is retained under the chief's corrective routing because this correction adds no motion.

Open for next:07 reruns corrective QA and build, including rendered failed-write feedback/retry where feasible; chief then re-gates. Preserve the explicit gaps: actual screen-reader pronunciation,200% zoom,reduced-motion runtime, M-01 shared cascade, public visual regression, StageProfile/Part W and broader state/connection acceptance. No merge, release or wave-completion authorization.06 stops here.

QA notes: Source confirms per-block lang is independent of active teaching preference, Home keeps saved-title locale, alerts are inside the modal content rather than only behind the modal, and selection failures retain the prior view before new state is applied. Coordinator separately reports rendered en/hi/bn block attributes, unknown/user blocks left untagged, Bengali Home heading language and no Guide horizontal overflow at seven widths; those are attributed runtime/geometry checks, not an independent06 accessibility walkthrough. No fresh automated suite, screen reader, zoom or reduced-motion run by06;07 owns final execution.
## 2026-09-23 — Internal mentor pipeline implementation

Done: Connected the existing authenticated shell to a content repository, a disconnected-but-injectable teaching interface, workspace-scoped cognition/evidence, structured L7 events, retained memory, and a resumable Home → Learn → Ask → Practice → Build path. Added a provisional syllabus/data-gap fallback, native-language activity switch, classwork-aware Home, evidence-backed memory card and privacy controls, and shared teacher/parent/professional/organization views. No generated model answer, official-curriculum claim, live sync, or real AGI claim was added. Files: `src/services/{contentRepository,teachingInterface,mentorStateService,learningPipelineService,roleMentorService,homeService,workspaceService}.ts`, internal dashboard pages/components, domain/hooks, and three new service-test files. Tests: 70/70 Node tests, strict domain TypeScript, and scoped internal JSX lint pass. Remaining gap: full lint/typecheck/build are blocked by concurrent public-page `PrivacyPage.jsx` parse errors and a public `TrustPage.jsx` unused import; public pages were not edited. Browser QA, production backend/model/consent/payment integration, and release gates remain open. Next: restore whole-app build outside this internal scope, then run rendered internal QA and connect real adapters.

Follow-up: Closed a workspace-switch stale-view risk in Learn/Guide and kept retained memory visible to its owner for deletion after personalization is disabled. The updated suite is 71/71; strict domain TypeScript and scoped internal JSX lint still pass. Whole-app build remains blocked by the unrelated public-page syntax error.

Onboarding correction: Removed automatic board/exam-to-subject invention and copy claiming textbooks/calendar are already configured. New learners may provide one starting subject or skip; existing saved subject lists are preserved. Files: `src/pages/Onboarding.jsx`, student onboarding steps/config, `src/lib/learningProfile.js`, `tests/workspace.test.mjs`. Tests: 72/72 Node tests and scoped onboarding lint pass. Remaining gap: official curriculum service and whole-app build/browser QA as above. Next: connect ContentRepository to the real syllabus source when available.

Onboarding follow-up: Fixed the existing board selector so it actually saves the selected board through `updateData`; previously it only toggled the state-field visibility and could not satisfy the step’s board requirement. File: `src/components/onboarding/steps/student/StudentContextForm.jsx`. Scoped lint passes; rendered step QA remains pending the unrelated public build repair.

Curriculum mapping follow-up: `ContentRepository.resolveProgressId` now keeps a mapped official concept attached to its prior provisional activity and updates the saved title on resume, instead of creating a duplicate unit. Files: `src/services/contentRepository.ts`, `src/services/learningPipelineService.ts`, `tests/learning-pipeline.test.mjs`. Focused tests and strict domain TypeScript pass. Remaining gap: the real syllabus adapter and rendered QA. Next: backend content mapping contract.

Daily-start/L7 follow-up: Saved onboarding board/class/subject context now supplies a student’s first Home action without making up ability, and structured learning/Ask events carry known curriculum context, pedagogy, and Ask intent without raw prompt text. Files: `src/domain/workspace.ts`, `src/services/{workspaceService,homeService,mentorStateService,learningPipelineService}.ts`, `tests/home.test.mjs`. Focused tests and strict domain TypeScript pass. Backend and whole-app rendered validation remain pending.

Class-to-Learn continuity: An enrolled student can open the class subject in Learn through a service-scoped context, and a selected chapter persists in the URL through refresh and unit navigation. The chapter presents one next concept plus a truthful started count, not inferred mastery. Files: `src/services/mentorStateService.ts`, `src/pages/dashboard/{StudentClasses,LearningWorkspace}.jsx`, `tests/role-mentor.test.mjs`. Tests: 75/75 Node, strict domain TypeScript, scoped lint pass. Remaining: public Privacy JSX still blocks whole-app build and browser QA; real content/model adapters remain disconnected. Next: rendered QA once the unrelated public build is repaired.

Class evidence follow-up: Class-launched learning units now retain their class ID through checks, practice and project application; the service validates active enrollment, class subject and concept placement before linking evidence. Only the assigned teacher's aggregate receives the verified result. Files: `src/services/learningPipelineService.ts`, `src/pages/dashboard/LearningWorkspace.jsx`, `tests/learning-pipeline.test.mjs`. Tests: 76/76 Node, strict domain TypeScript, scoped lint pass. Remaining: production backend authorization and rendered QA; no live model or curriculum claim.

Rendered continuity check: The concurrent public-page syntax issue was fixed outside this internal work. Production build and whole-app typecheck now pass. Playwright verified Aarav's connected class → Geometry provisional outline → Chapter 1 → unit → outline return at desktop/mobile widths; chapter survives refresh, the mobile next action is visible above bottom navigation, and 390px has no horizontal overflow. Final 76/76 Node tests and scoped lint pass. Whole-app lint has one unrelated public `TrustPage.jsx` unused import. Remaining: broader role/accessible-device QA and live content/model/backend integrations.

Internal design pass: Kept the existing blue/white identity while tightening the shared workspace frame, navigation targets and labels, role Home priority hierarchy, mobile Ask composer order, Profile forms, and empty usage progress styling. Removed duplicated teacher/parent/organization Home sections without removing their role panels or routes. Files: internal dashboard shell, `workspace.css`, `DecisionHome.jsx`, `Guide.jsx`, `Profile.jsx`, and `homeService.ts`. Checks: 76/76 Node tests, typecheck, production build, scoped lint, and rendered five-role mobile Homes plus teacher desktop/Ask/Profile/Plans; Ask draft refresh and send pass. Remaining: whole-app lint reports one unrelated public `TrustPage.jsx` unused import; full accessibility/device matrix and later backend/model connection remain open. Next: founder-directed internal product work.

Learn/Practice entry follow-up: Simplified Learn to one subject field with optional board/class disclosure, and made empty Practice lead directly to Learn instead of repeating setup fields. File: `src/pages/dashboard/LearningWorkspace.jsx`. Focused learning-pipeline tests (7/7), typecheck, scoped lint, and 390px route/content/overflow checks pass. A concurrently edited public `src/pages/landing/PrivacyPage.jsx` acquired mismatched JSX closing tags during final validation, so the latest whole-app build and clean browser screenshot are blocked outside this internal scope; the earlier successful build predates this public edit. Next: repair public syntax in its owning workstream, then rerun whole-app build and visual QA.



Daily Mentor Engine — daily plan module: Implemented the founder-directed daily planning seam over existing services only. `src/services/dailyPlanService.ts` composes one sequenced day from recorded evidence — published classwork (enrollment-checked), one due SCM review (deduplicated against the open unit), the most recent unfinished learning unit, unfinished project work, and work already verified today shown as closed. Empty evidence yields an empty plan; no weakness, ability, or curriculum is invented, and non-learner roles and minors in professional workspaces are refused. Home now shows this as one "Today's plan" module (`homeService.ts`), replacing the scattered classwork/build module rows; the priority card keeps its existing honest selection logic. Files: `src/services/dailyPlanService.ts`, `src/services/homeService.ts`, `tests/daily-plan.test.mjs`, `scripts-tmp/plan-home-smoke.mjs`. Tests: 81/81 Node (5 new), both typechecks, production build, scoped internal lint; whole-app lint still blocked only by the public `TrustPage.jsx` unused import. Rendered smoke: plan module with classwork row renders at 1440/390 with zero page errors and zero overflow; evidence-free learner correctly gets no plan module. Remaining gap: plan is read-only (no completion marking/snooze), professional-role plan uses the same learner composition behind a career goal rather than a dedicated sequence, and backend daily-plan personalization connects later through the same seam. Next: rendered role-matrix QA or teacher-side plan adoption, then backend adapters.

M-01 closure: The shared reduced-motion smooth-scroll defect is fixed as pre-authorized — `src/index.css` reduced-motion `html{scroll-behavior:auto}` now carries `!important` so the later public `html{scroll-behavior:smooth}` can no longer override it. One-line scoped change; the public smooth-scroll behavior itself is untouched (verified by diff: 3 insertions, 1 deletion, no other rule changed). Runtime screen-reader/zoom certification remains open as before.

Audio presence seam (always-ready audio interaction): Implemented real voice interaction using the browser's own speech engine — no backend needed and no simulated listening. `src/services/voiceService.ts` owns one honest state machine (off/listening/speaking/denied/unsupported) over Web Speech recognition and synthesis: continuous listening with interim captions, half-duplex (the microphone pauses while the mentor speaks and resumes after), locale mapping (en/hi/bn → en-IN/hi-IN/bn-IN), immediate microphone release and a sticky-but-retryable denied state on permission refusal, and graceful unsupported handling. A spoken turn takes the exact teaching-seam path as typed text: `sendTeachingTurn` gained an optional `inputType` (default 'text') so L7 events record `input_type: 'voice'` while still carrying no raw transcript. New `VoiceDock` in the dashboard shell is the always-ready companion — voice mode defaults ON (founder setting; legacy workspaces normalize to on via `preferences.voice ??= true` in `snapshot`), the microphone starts on the first tap because browsers require a gesture, replies are read aloud, and a card shows the transcript, the reply with its honest status label, and thread links. Ask's composer gained a dictation button that appends finalized speech to the draft without altering its QA'd focus/tab semantics, and Guide speaks responses when voice is on. The personalization panel carries the off-switch (fully stops the dock). Crisis hard-stop applies to spoken input identically. Files: `voiceService.ts`, `VoiceDock.jsx`, `DashboardLayout.jsx`, `Guide.jsx`, `WorkspaceTools.jsx`, `workspaceService.ts`, `learningPipelineService.ts`, `domain/workspace.ts`, `workspace.css`, `tests/voice.test.mjs`, `scripts-tmp/voice-home-smoke.mjs`. Tests: 87/87 Node (6 new), both typechecks, production build pass; whole-app lint still blocked only by the public `TrustPage.jsx` unused import. Rendered smoke: dock states honest at 1440/390 with 0 page errors and 0 overflow; headless denial showed the truthful denied state; dictate button present; off-switch removes the dock. Remaining gap: recognition quality/voice selection is the browser's (Chrome sends audio to its speech service — the UI copy says "browser transcribes"), no wake-word, no barge-in during speech, and the intelligence behind replies remains the disconnected teaching seam until the model connects. Next: real model adapter or wake-word/barge-in refinement on founder direction.

Audio presence redesign (founder directive — no assistant persona): Removed the assistant-style orb dock entirely (VoiceDock.jsx deleted) along with any persona naming from product records. The AGI's presence is now `src/components/dashboard/AudioPresence.jsx` — a thin audio-reactive horizontal line fixed at the very top of the interface, with no icon, avatar, or activation button. Activation is automatic when the product opens: if the microphone is already permitted, listening begins immediately; on a first visit it begins on the first interaction (browsers require a gesture before a permission prompt); a blocked or unsupported browser keeps the line honestly static. While listening the line reacts to the real microphone level through a getUserMedia + AnalyserNode capture (falling back to a gentle state-only breath when the analyser is unavailable); while the AGI speaks it carries the reply cadence; when audio is off it stays a quiet static gradient indicator that never suggests active listening, and a screen-reader status states this explicitly. A live caption pill appears only while speech is being heard. Spoken turns still ride the teaching seam: finals are debounced on a 1.5s silence so a complete thought is sent, replies are spoken, and `input_type: 'voice'` events carry no raw transcript. Settings' personalization panel now names the option "Audio Interaction" with copy that distinguishes the active-microphone line from the availability-only line; Ask gained a session-scoped audio on/off quick control (`voiceService.setSessionAudioOverride`, resets on reload, overrides the saved preference both ways, verified by test). The composer dictation button remains as an input affordance. Files: `AudioPresence.jsx` (new), `VoiceDock.jsx` (deleted), `DashboardLayout.jsx`, `Guide.jsx`, `WorkspaceTools.jsx`, `voiceService.ts`, `workspace.css`, `tests/voice.test.mjs`, `scripts-tmp/voice-home-smoke.mjs`. Tests: 88/88 Node (7 voice incl. session override), both typechecks, production build pass; whole-app lint still blocked only by the public `TrustPage.jsx` unused import. Rendered smoke: line + canvas present with zero old-dock remnants, honest status strings for on-but-inactive and off states, Ask toggle true→false, Settings toggle default-on, 0 page errors, 0 overflow at 1440/390; strip capture confirms the subtle gradient line renders. Remaining gap: no wake-word or barge-in, recognition quality is the browser's, and the intelligence behind replies remains the disconnected teaching seam until the model connects. Next: model adapter, or workflow-depth features (goals, follow-up questions, proactive next steps) on founder direction.

Mentor companion layer (founder directive — continuously active, category-specific AGI): Implemented the always-available companion over the real product records, on the existing presence/audio architecture. `src/services/mentorCompanionService.ts` is one category-aware intelligence surface: (1) `buildMentorPacket` assembles the full context seam the future model consumes — role, locale, learning profile, today's plan steps, top SCM concepts, weekly observations, open-project count, recent conversation titles, `promptVersion: 'mentor-context-v1'` — never emitted into L7 events; (2) `sendMentorTurn` routes a natural-language request (typed or spoken) to real actions over saved records via a deterministic multi-language intent router (en/hi/bn: today's plan, continue, practice/review, classwork/classes, build/portfolio, progress, open-a-section, greetings) and answers with localized text plus executable action blocks and an explicit "From your saved records — not an AI assessment" source line; category specialization is real — the same "open my classwork" request reaches student classwork, a teacher's assigned-class evidence, a parent's shared reports, or organization insights; (3) open-ended questions fall through to the existing teaching seam unchanged (honest `not_connected`, exactly one message pair); (4) `mentorGreeting` powers a new proactive check-in on the Ask surface (`MentorGreeting.jsx`): one evidence-based next step with actions, localized, offered — never auto-executed. Guide now sends through the companion (status `mentor`, new domain Message status, labeled "From your saved records"); speaking respects the session audio override. The smoke caught and fixed two real defects: spoken replies constructed a plain object instead of a browser `SpeechSynthesisUtterance` (now built through the real constructor when present — regression-tested), and the session audio control did not silence spoken replies (now routed through `resolveAudioEnabled`). Files: `mentorCompanionService.ts`, `MentorGreeting.jsx`, `workspaceService.ts` (`appendGuideBlocks`), `domain/workspace.ts`, `Guide.jsx`, `voiceService.ts`, `tests/mentor-companion.test.mjs`, `tests/voice.test.mjs`, `scripts-tmp/voice-home-smoke.mjs`. Tests: 95/95 Node (6 companion + 1 utterance regression), both typechecks, production build pass; whole-app lint still blocked only by the public `TrustPage.jsx` unused import. Rendered smoke: mentor turn with audio on shows reply + action + zero error panels; greeting/category/localization asserts pass; 0 page errors, 0 overflow at 1440/390. Remaining gap: the companion's answers are deterministic compositions of saved records (the router and context seam accept the real model without rewrite); no cross-session proactive scheduling (e.g., notifications), no free-form task completion yet. Next: model adapter into the packet seam, or proactive scheduling on founder direction.

Internal interface design completion: Unified blue accent (#4285F4), readable action/link blue (#0b57d2), error foreground/container/border, responsive page spacing, list wrapping and portal tokens. Refined account identity/plan groups, navigation contrast, workspace selector, dialog sizing/focus and reduced-motion behavior; legacy blue controls inherit the same contrast treatment. Files: dashboard workspace.css, DashboardTopbar.jsx, DashboardSidebar.jsx, WorkspaceSwitcher.jsx, hooks/useThemeColor.jsx. Verification: production build, typecheck and scoped lint pass; five role Homes at 360/1440 plus role entry pages render without errors; shared layout checked at 360/390/768/1024/1280/1440/1920, account focus restoration, mobile search width, and real error-state colors verified. Full suite: 94/95, unrelated daily-plan completion test fails; full lint retains public TrustPage unused import. Existing 5174 Vite cache returns outdated dependency errors; isolated 5182 preview used for QA. Remaining: functional issues belong to the next functionality pass.
2026-09-24 — Mentor connection seam and launch handoff. Done: aligned learning timestamps with the injectable workspace clock; added an injectable, fail-closed mentor-model contract, role-scoped action IDs, cancellation, draft preservation, privacy-gated conversation context, and safety priority over ordinary intent routing. Files: `src/services/{workspaceService,learningPipelineService,mentorCompanionService,mentorModelService}.ts`, `src/domain/workspace.ts`, `src/pages/dashboard/Guide.jsx`, `tests/mentor-model.test.mjs`, `docs/visionary/AGI_LAUNCH_SEQUENCE.md`. Checks: 100/100 Node tests, typecheck, build, changed-file lint pass. Whole-repo lint still fails only on unrelated public `TrustPage.jsx` unused `Link`. Remaining: no actual model/backend endpoint, no server authorization/sync, and no real AGI claim. Next: Gate 1 authenticated transport contract from `AGI_LAUNCH_SEQUENCE.md`, then connect only to supplied endpoint/schema.
2026-09-24 follow-up — Full `npm run lint` passed on recheck after a concurrent public-page change; this internal pass did not edit that page. All four gates now pass: 100 tests, typecheck, build and full lint. Resume pointer remains Gate 1 in `AGI_LAUNCH_SEQUENCE.md`.
2026-09-24 — Gate 1 authenticated transport boundary. Done: `backendTransport.ts` now installs the existing content, teaching and mentor seams as one opt-in connection, checks the authenticated account/workspace/role before and after requests, rejects expired or changed sessions and late replies, supports cancellation, and keeps the browser-local preview when disconnected. It invents no endpoint, credential or model output. Files: `src/services/backendTransport.ts`, `tests/backend-transport.test.mjs`, launch sequence. Checks: 105/105 Node tests, typecheck, full lint and production build pass. Remaining: server-side authorization, real transport/schema, owner-scoped data migration and cross-device sync. Next: Gate 1 migration dry-run with explicit owner mapping, then real connection only after backend contracts arrive.
2026-09-24 — Gate 1 local migration preview milestone. Done: added a read-only per-person inspector for versioned workspace, curriculum, learning and cognition stores; it counts all owned personal roles, excludes other people's data, and blocks mismatched evidence owners, duplicate workspace IDs, unassigned records and connected organization spaces from automatic mapping. Relationships require renewed permission. Privacy now offers an honest, user-triggered local-data review with no upload/delete/merge. Files: `src/services/localMigrationService.ts`, `src/components/dashboard/LocalDataPreview.jsx`, `src/pages/dashboard/WorkspaceTools.jsx`, `tests/local-migration.test.mjs`, `scripts-tmp/local-data-preview-smoke.mjs`, launch sequence. Checks: 111/111 Node tests, typecheck, full lint, production build, rendered Privacy at 1440/390 with zero page errors/overflow. Remaining: no verified target-owner mapping, real migration, server authorization or cross-device sync. Next: Gate 1 target-owner mapping dry-run; retain all local originals until authenticated backend reconciliation is specified and verified.
2026-09-24 — Gate 1 target-map proposal complete. `planLocalMigrationMapping` now requires every owned personal workspace exactly once with matching roles and unique targets, refuses unresolved local ownership, and returns only a dry-run requiring server verification and user confirmation. No record is uploaded, merged or deleted. Files: `src/services/localMigrationService.ts`, `tests/local-migration.test.mjs`, launch sequence. Checks: 113/113 Node tests, typecheck, full lint and production build pass; earlier rendered Privacy check still covers the unchanged UI. Remaining: authenticated target verification, transfer reconciliation and sync require real backend contracts. Next independent implementation: Gate 2 curriculum provenance and language availability in ContentRepository/Learn; continue Gate 1 server work once backend contracts are supplied.
2026-09-24 — Gate 2 curriculum provenance/language milestone. Connected curriculum now requires provider/source ID/version, a supported content locale and per-concept language availability; invalid responses cannot replace saved graphs. Separate language variants coexist under stable concept IDs. Learn shows source/version and honestly flags source-language fallback; switching languages clears stale explanations/questions/model responses without erasing the unit or evidence, and fetches a newly available variant through the same repository seam. Provisional outlines and authored samples remain usable. Files: `src/services/{contentRepository,learningPipelineService}.ts`, `src/pages/dashboard/LearningWorkspace.jsx`, `tests/{content-teaching,learning-pipeline}.test.mjs`, `scripts-tmp/content-language-smoke.mjs`, launch sequence. Checks: 117/117 Node tests, typecheck, full lint and production build pass; rendered Learn at 1440/390 showed provenance and Hindi gap, no English explanation leak, page error or overflow. Remaining: real sourced GCP feed, native-speaker/pedagogical review and production authorization. Next: Gate 3 Daily Mentor loop closure and failure-state QA; avoid reworking this contract unless a concrete regression or backend incompatibility is found.
2026-09-24 — Gate 3 Daily Mentor priority increment. Home's primary action now derives from the same scoped pending steps as Today's plan, while a recent unfinished conversation remains resumable unless connected classwork is already due; classwork with no due date is still actionable. `learningPriority` uses the injected workspace clock so due recommendations cannot drift from the plan's recorded day. Files: `src/services/{homeService,learningPipelineService}.ts`, `tests/daily-plan.test.mjs`, launch sequence. Checks: 119/119 Node tests, typecheck, full lint and production build pass. Remaining: failure-state and rendered cross-role loop closure; real content/model/backend authorization still unconnected. Next: Gate 3 recovery/scoping QA, then Gate 4 connected teaching contract hardening.
2026-09-24 — Gate 3 local closure. Build creation now recovers the same artifact after a partial learning-store write, and completion retries do not duplicate application evidence or L7 save/complete events; the daily-plan test uses the same injected clock for evidence, plan and workspace. Files: `src/services/learningPipelineService.ts`, `tests/{learning-pipeline,daily-plan}.test.mjs`, `scripts-tmp/role-home-smoke.mjs`, launch sequence. Checks: 121/121 Node tests, typecheck, full lint, production build and diff check pass; rendered student plan and all five role Homes at 1440/390 have zero page errors, alerts or horizontal overflow. Remaining: actual backend/model/curriculum, server authorization and broader accessibility certification. Next: Gate 4 contract hardening; no endpoint invention or local answer simulation.
2026-09-24 — Gate 4 timeout increment. The existing authenticated backend transport now applies one configurable, bounded deadline across both session checks and the content/teaching/mentor exchange, aborts the downstream signal on timeout, refuses late replies and keeps the prior saved Ask draft/work unchanged. Files: `src/services/backendTransport.ts`, `tests/backend-transport.test.mjs`. Checks: 123/123 Node tests, typecheck, full lint, production build and diff check pass. Remaining: real model endpoint/schema, server-side grounding/safety, multilingual model evaluation and live auth are not supplied. Next: tighten typed response/authorization contracts against the same seam; no local answer simulation.
2026-09-24 — Gate 4 client teaching contract. Connected teaching replies now require matching locale and a nonempty response version, pair status with the correct source, bound text/representation size, and save only allowlisted response/question/visual fields. Safety blocks must use the requested language; English disconnected copy is never relabelled. Learn and Ask L7 response events record a privacy-safe version reference without prompt text. Files: `src/services/{teachingInterface,learningPipelineService}.ts`, `tests/{content-teaching,backend-transport,learning-pipeline}.test.mjs`. Checks: 125/125 Node tests, typecheck, full lint, production build and diff check pass. Remaining: real endpoint, server grounding/safety, multilingual content/model evaluations and auth are external gates. Next independent build: Gate 5 connected teacher/parent/professional/organization workflow depth over existing shared services; do not claim a real AGI or restart the student loop.
2026-09-24 — Gate 5 teacher class-scope increment. Teacher class selection, learner evidence and reviewed-lesson assignment now use the same explicit service-level ownership filter; even an over-broad future repository response cannot place another teacher’s class or submission in the view. The assignment picker consumes that scoped service, and a second teacher cannot assign into the first teacher’s class. Files: `src/services/classroomService.js`, `src/components/dashboard/AssignLesson.jsx`, `tests/workspace.test.mjs`. Checks: 125/125 Node tests, typecheck, full lint, production build and diff check pass. Remaining: this is a client/preview guard, not server authorization; quiz/lesson scaffolding, parent/professional/organization workflow depth and real backend remain. Next: complete the teacher preparation-to-assignment experience without inventing model-generated questions, then progress through other role adapters.
2026-09-24 — Gate 5 teacher-authored check loop. A reviewed lesson can now contain up to ten validated short-answer questions; the assignment receives an immutable, answer-key-free copy. Learners answer each question in their own words, drafts survive refresh on this device, and a scoped/idempotent submission service enforces active enrollment and complete answers. Existing teacher feedback remains manual; no model-generated questions or automatic mastery are claimed. The Assign control refuses unsaved/stale lesson versions. Files: `src/domain/workspace.ts`, `src/services/workspaceService.ts`, `src/services/classroomService.js`, `src/components/dashboard/{LessonChecksEditor,AssignLesson}.jsx`, `src/pages/dashboard/{WorkspaceTools,StudentClasses}.jsx`, `tests/workspace.test.mjs`, `scripts-tmp/teacher-check-smoke.mjs`. Checks: 125/125 Node tests, typecheck, full lint, production build, diff check and rendered teacher→learner flow at desktop/mobile with draft refresh and no page errors/overflow pass. Remaining: server authorization/sync, broader accessibility review, richer question types and other role-depth milestones. Next: professional goal→evidence→portfolio and parent/organization role flows over the same services.
2026-09-24 — Gate 5 professional path. A professional now sets one personal career direction and explicitly links it to an owned capability; the career view joins that learning unit’s SCM evidence and private portfolio artifact without a hiring or mastery claim. The local authored data-interpretation sample runs through Learn→Check→Practice→Build in English/Hindi/Bengali, separate from the school mathematics sample. Home reflects a saved target but an open lesson still takes priority; the app bar follows the active role and no longer says “Join a class” for professionals. Files: `src/domain/workspace.ts`, `src/services/{contentRepository,roleMentorService,homeService}.ts`, `src/pages/dashboard/{LearningWorkspace,ProfessionalCareer,WorkspaceTools}.jsx`, `src/components/dashboard/DashboardTopbar.jsx`, `tests/role-mentor.test.mjs`, `scripts-tmp/professional-career-smoke.mjs`. Checks: 126/126 Node tests, typecheck, full lint, build, diff check, and desktop/mobile browser journey with no page errors/overflow pass. Remaining: real model/content, server authorization/sync, unsaved form-draft recovery, wider accessibility audit. Next: consent-scoped parent support and organization cohorts/aggregate workflows.
2026-09-24 — Gate 5 parent/organization connection hardening. A parent report deep link now fails closed when its child is no longer authorized instead of silently displaying another child; the selected child updates the URL. Existing organization cohorts now refuse cross-resource edits and malformed member/class lists, count only currently connected records, refresh their roster, and let an admin remove a revoked member/class reference without deleting the saved cohort. No cohort membership grants private learner data. Files: `src/pages/dashboard/{WorkspaceTools,Cohorts}.jsx`, `src/services/classroomService.js`, `tests/organization-cohort.test.mjs`, `scripts-tmp/{parent-report,organization-cohort}-smoke.mjs`. Checks: 127/127 Node tests, typecheck, full lint, build, desktop/mobile parent and organization browser flows with no page errors/overflow. Remaining: reviewed content distribution and richer organization reporting, cross-device consent/permissions, independent accessibility and safety review. Next: finish remaining role workflow gaps and integration contracts, then release hardening; server and model work remain external gates.
2026-09-25 — Presence relocated into the search bar (founder directive). The AGI's ambient presence is no longer a line at the top edge: `AudioPresence` now renders a capsule of light around the workspace search pill (`DashboardTopbar` anchors it inside the search button wrapper; the fixed-top container was removed). The ring reacts to the real microphone level via the existing getUserMedia+AnalyserNode capture while listening, carries the reply cadence while speaking, and stays a quiet static ring when audio is off — the screen-reader status states the off-state explicitly and reduced-motion users get the static ring. Auto-activation is unchanged (permission granted → listening starts on open; first visit → first gesture; denial → honest static with retry-on-grant), captions surface under the search pill while speech is heard, and spoken turns keep riding the mentor-companion/teaching seams with `input_type:'voice'` and no transcript in events. Files: `src/components/dashboard/AudioPresence.jsx`, `src/components/dashboard/{DashboardTopbar,DashboardLayout}.jsx`, `src/components/dashboard/workspace.css`, `scripts-tmp/voice-home-smoke.mjs`. Checks: 127/127 Node tests, both typechecks, full clean lint (the public TrustPage blocker is resolved), production build, and a rendered smoke: ring present on the search pill with zero old top-line/dock remnants, honest on-inactive and off status strings, mentor turn with audio on shows reply + action with zero error panels, session audio control and the Audio Interaction setting still govern it, 0 page errors and 0 horizontal overflow at 1440/390 (captures: audio-presence-home.png, ring-strip.png, mentor-turn.png).
2026-09-25 — Handoff state for the team (state of the union after the voice/presence work): landed on top of your Gates 1–5 and committed in v1.0.59–v1.0.63 — (1) `dailyPlanService` Today's plan with Home integration; (2) `voiceService` (honest listening/speaking/denied/unsupported machine, half-duplex, en/hi/bn) with automatic activation and the search-bar presence ring; (3) `mentorCompanionService` (`buildMentorPacket` context seam you already consume in `mentorModelService`, `sendMentorTurn` en/hi/bn intent router to real actions, proactive `mentorGreeting` on Ask, Message status `mentor` labeled "From your saved records"); (4) two fixed defects worth knowing: spoken replies must be constructed through the browser's `SpeechSynthesisUtterance` constructor (plain objects are rejected — regression in tests/voice.test.mjs), and session audio must route through `resolveAudioEnabled` so the Ask session control silences speech too. Open where you left off: your recorded next steps stand — Gate 4 connected teaching contract hardening, Gate 1 server work when backend contracts arrive; voice-specific gaps are no wake-word/barge-in (recognition is the browser's speech service) and no cross-session proactive scheduling; internal community module remains unbuilt. Nothing here invents endpoints, model output, or curriculum: the presence and companion layers ride your transport boundary and fall back to the local preview honestly.
2026-09-25 — Listening animation made visible + Google-spec navigation pills (founder review fixes). (1) The presence ring previously drew an almost-invisible thin line in every state; it now renders like the Apple bar animation while listening: a flowing deep-to-light blue gradient wave (5.5px glow pass with shadow + 1.8px core) whose amplitude is always alive at a visible floor and swells with the real microphone level, carrying the reply cadence while speaking; the audio-off state keeps a quiet static ring that cannot be mistaken for listening, and reduced-motion users get the static ring plus status text. (2) DashboardSidebar rebuilt to Google navigation spec: pill states everywhere (rounded-full), a neutral #eceef1 hover with #121317 content — blue is reserved for the selected item (#e8f0fd pill + #0b57d2 icon/label) — and in the collapsed rail the active pill wraps only the icon with the label below, Gmail-style. Files: `AudioPresence.jsx`, `DashboardSidebar.jsx`. Checks: 127/127 Node tests, typecheck, clean lint, production build, and a new visual script `scripts-tmp/ring-visual.mjs` that captures the real listening animation in headless (mic granted + recognition runtime injected via Object.defineProperty — plain window assignment does not shadow the native API, worth remembering), proving visible motion between frames (`ring-listen-1/2.png`) against the honest static ring (`ring-static.png`), plus sidebar hover/active and collapsed icon-pill captures (`sidebar-states.png`, `sidebar-collapsed.png`). Remaining: live-mic pixel verification on real hardware, and the wider a11y certification as recorded.
2026-09-25 — Founder review round: presence visibility, filled nav icons, onboarding Enter, illustration ratios. (1) **AGI visibility**: the ring previously sat near-invisible whenever the microphone was not yet granted (the common first-visit state), which read as "no AGI on the page". The capsule now has three honest tiers — the full flowing wave while listening/speaking; a clearly visible slow "ready" breathing (gentle glow, small amplitude, ~2s cycle) whenever audio interaction is enabled but the microphone is not active (waiting for permission, blocked, between turns), communicating available-and-ready without a listening claim per the founder's original directive; and a quiet static ring only when audio is switched off. Screen-reader status text stays truthful in every tier. (2) **Navigation**: the founder rejected the bold-stroke idea — the selected item's icon is now SOLIDLY filled with its own blue (fill=currentColor, full opacity) and the label renders in light blue #4285F4 on the existing #e8f0fd pill; non-active items stay #121317 with neutral #eceef1 hover pills. (3) **Onboarding Enter**: OnboardingLayout and IdentitySelection are real forms now — Enter in any text field submits and continues (textareas unaffected); the Continue buttons are type=submit with honest disabled states. (4) **Illustrations**: the portrait spots (student/teacher/parent/team, 120×160) were being sliced into square 40×40/80×80 boxes, cropping them badly; they now render in exact 3:4 tiles (72×96 on identity cards, 96×128 on step headers) with no cropping, and squares keep 1:1. Files: `AudioPresence.jsx`, `DashboardSidebar.jsx`, `OnboardingLayout.jsx`, `IdentitySelection.jsx`, `scripts-tmp/fix-verify.mjs`. Checks: 127/127 Node tests, typecheck, clean lint, production build, rendered verification — presence breathing frames (`presence-ready-1/2.png`), filled-icon sidebar (`sidebar-bold.png`), identity illustrations (`onboarding-identity.png`), and the behavioral assert `enterAdvancesToStep2=true` (typed in the name field, pressed Enter, page continued) with 0 page errors.
2026-09-25 — Founder final form: the AGI is a Siri-style orb inside the search bar. The capsule ring around the search pill is removed. `AudioPresence` now renders a small always-animating orb positioned inside the search bar on the right, mirroring the search icon on the left: a deep sphere with swirling teal/pink/violet energy ribbons around a glowing core (canvas, 'lighter' compositing). The listen animation is always running, and brightness/speed carry the honest state — calm when audio is enabled but the microphone is not active, bright and fast while listening with the core swelling on the real microphone level, cadence speed while speaking, dimmed only when audio is switched off (screen-reader status remains truthful per tier; reduced-motion users get a still orb plus status text). Auto-activation, silence-debounce spoken turns, and `input_type:'voice'` event privacy are unchanged. Also: the selected sidebar icon keeps its border — the fill is now two-tone (#4285F4 interior, #0b57d2 stroke) because a same-color fill hid the icon's border (founder correction). Files: `AudioPresence.jsx`, `DashboardSidebar.jsx`, `workspace.css`. Checks: 127/127 Node tests, typecheck, clean lint, production build, and rendered captures — the orb visible inside the search bar in ready and listening states (`presence-ready-1/2.png`, `ring-listen-1.png` via the injected-recognition context) and the bordered filled icon (`sidebar-bold.png`); onboarding Enter (`enterAdvancesToStep2=true`) and identity illustrations re-verified, 0 page errors.
2026-09-25 — AGI orb identity + navigation finalized per founder references. (1) The orb is no longer a Siri clone: it is Visionary's own signature — a deep-blue sphere with swirling blue-family energy (#8ab4f8 / #4285F4 / #174ea6), a glowing white-blue core, and a thin light "guiding orbit" ring sweeping around the core (the mentor circling the learner); the listen animation is always running with honest tiers (calm ready, bright+fast listening swelling with real mic level, cadence speaking, dimmed audio-off). Placement was deliberately evaluated and kept inside the search bar's right side mirroring the search icon — the same pattern Google uses for its AI spark in Search: present on every page, no extra chrome, no assistant clichés, nothing for users to learn. (2) The clicked navigation item now matches the founder's Google Skills reference exactly: the glyph renders SOLID filled (uniform fill, fill=currentColor) on the light-blue pill with the blue label; non-active items stay outline + #121317 with neutral hover pills. Files: `AudioPresence.jsx`, `DashboardSidebar.jsx`. Checks: 127/127 Node tests, typecheck, clean lint, production build; rendered captures re-verified (`presence-ready-1/2.png` blue orb, `sidebar-bold.png` solid glyph, onboarding `enterAdvancesToStep2=true`, 0 page errors).
2026-09-25 — "No change visible" resolved: production-bundle verification + mobile orb fix. The founder could not see the described changes; root causes found and fixed. (1) **Mobile placement bug**: the presence anchor stretched to `max-w-xs` on narrow widths while the search pill was only 44px, so the orb floated detached from the pill in empty topbar space — on a phone it looked like nothing existed. The search pill is now a proper mini-pill at every width (w-20 on mobile with the search icon left and the orb right; full-width pill on sm+), verified geometrically in the production bundle (`insidePill=true` at 1440 and 390). (2) The orb was enlarged (28px desktop, 22px mobile) for presence. (3) Verification is now done against the **built app via `vite preview`** (`scripts-tmp/prod-verify.mjs`, seeded local session) rather than only the dev server, with production screenshots `prod-home-1440.png` / `prod-home-390.png` as evidence; 0 page errors. Team note: when a founder reports unseen changes, first confirm which bundle they are viewing (dev 5173 vs preview/dist vs deployed domain) and have them hard-refresh; stale bundles mimic missing work.
2026-09-25 — Google-flat page design + orb relocation (founder design review). Analysis from full-page captures: the workspace was double-framed — blue-gray canvas (#f6f8fc) with a white rounded 20px bordered "surface" card floating on it, gray gaps after the topbar and after the sidebar, an oversized 44px avatar, and the AGI orb inside the search bar where users could confuse it with search. Fixes: (1) the shell is now one continuous **flat white surface** (`.workspace-shell` white, `.workspace-surface` transparent with no border/radius); section-level `v-card` boxes remain as the only cards, like Google's own settings pages; the sidebar gains a single hairline `#eceef1` divider; the topbar keeps its one hairline. (2) The account avatar is 36px (was 44px). (3) **The AGI orb left the search bar** and is now a clear 40px topbar orb button in the right utility cluster (between connections and help) — tapping it opens the AGI conversation in Ask, the same unambiguous pattern as the Gemini button in Google Workspace; the orb still animates always (calm ready / bright listening with real mic level / cadence speaking / dimmed audio-off) and listening still activates automatically. Captures: before (`current-home-1440.png`) vs after (`after-home-1440.png`, `after-home-390.png`); orb click verified to navigate to `/dashboard/ask` (`after-orb-ask.png`). Files: `workspace.css`, `DashboardTopbar.jsx`, `DashboardSidebar.jsx`, `AudioPresence.jsx`, `scripts-tmp/design-capture.mjs`, `scripts-tmp/design-after.mjs`.

2026-09-25 — Function work resumed: **internal community module, service layer**. `src/services/communityService.ts` implements the founder's community requirement with a safety-first shape: the community is **class-scoped** — learners read/post only inside classes where their enrollment is active, the assigned teacher reads/posts and can remove any post (moderation recorded with `removedById`), and parent/organization roles are refused at the service level. Posts are validated (1–1000 chars), stored per class, and every action emits honest L7 events (`app: 'COMMUNITY'`, actions `view`/`save`/`remove`) that carry **no post text**. The L7 schema gained the additive `COMMUNITY` app and `remove` action (`mentorStateService.ts`). Tests (`tests/community.test.mjs`, 3): scoping across five roles including wrong-class teacher and enrollment denial, posting/validation/moderation flow with removal hidden from learners, and per-workspace event privacy (student save vs teacher remove never mix, no post text in events). Checks: 130/130 Node tests, typecheck, clean lint, production build. Remaining: community UI inside ClassDetail (posts list + composer + teacher moderation controls), rate limiting, and backend connection — UI wiring is the immediate next step.
2026-09-25 — Layout finalized per founder: no separator lines anywhere except the working-surface rectangle. The topbar's border-b and the sidebar's divider are removed — the app bar, nav rail and canvas are pure white with no lines. The main working/learning screen is the ONLY bordered element: `.workspace-surface` renders as a 16px-rounded rectangle (#eceef1 1px) floating on the white canvas with an even gutter (main gained top padding to match the sides/bottom). The AGI orb button moved out of the app bar into the nav rail, below the navigation items (bottom of the sidebar; it also appears in the mobile navigation sheet, which contains the same sidebar component), and still navigates to `/dashboard/ask` on tap — verified in the production bundle. Files: `DashboardTopbar.jsx`, `DashboardSidebar.jsx`, `DashboardLayout.jsx`, `workspace.css`. Checks: 130/130 Node tests, typecheck, clean lint, production build, capture `current-home-1440.png` (lineless chrome, rectangle working surface, orb below nav) and click-through evidence.
2026-09-25 — AGI orb finalized: bigger, always-on, summon-not-navigate (founder directive). The orb is now 48px on a light-blue circular backdrop (#e8f0fd, hover #d7e3fd) below the navigation, with a 36px sphere, a persistent breathing **always-on halo** that tells any user the intelligence is on (the Gemini/Hey-Google on-state pattern), and a one-time **turn-on bloom** when the product opens (scale-in entrance + halo flash; reduced-motion renders it still). **Tapping it no longer navigates anywhere** — this is not a chatbot: the AGI turns toward the user and announces itself, speaking "I am your Intelligence — for you, always available." (localized en/hi/bn) through the browser's speech synthesis when audio is on, and showing the same line as a caption either way; the caption clears when the spoken line ends (or after 4.5s when silent). A summon emits an honest L7 event (app ASK, action start, inputType system — no text). One regression was caught and fixed in the production-bundle check: a stray `useNavigate` reference crashed the sidebar after the import was removed (proof that production-bundle verification belongs in every round; the verify run now includes page-error capture). Files: `AudioPresence.jsx`, `workspace.css`. Checks: 130/130 Node tests, typecheck, clean lint, production build; rendered verification in `vite preview` — summon caption appears with the exact line, URL unchanged, caption clears, 0 page errors (`orb-final.png`).
2026-09-25 — Founder polish round: visible border, conditional switcher, aurora orb. (1) The working-surface rectangle border darkened from invisible #eceef1 to the product's standard #dadce0. (2) The workspace switcher ("Learner space ▾") now renders **only when more than one workspace exists** (class/organization/extra-role connections); a single-workspace user sees no arrow that leads nowhere — trade-off noted: the "+ Add a role" option lives inside this switcher, so a single-workspace adult loses that affordance until a second workspace exists (verified hidden=0 for one workspace, visible=1 for two). (3) The AGI orb animation was redesigned to the founder's reference image (Bing/Copilot-style luminous band): a light-blue disc (no black anywhere) with flowing saturated blue/violet aurora bands, a drifting white-blue luminous heart, the always-on halo pulse, and the turn-on bloom retained; motion amplitude swells with the real microphone level while listening. The summon caption now displays for a fixed 4.5s even when headless speech ends instantly (verified: caption present at 120ms after click with the exact line). Files: `workspace.css`, `WorkspaceSwitcher.jsx`, `AudioPresence.jsx`. Checks: 130/130 Node tests, typecheck, clean lint, production build; rendered captures `aurora-final-1/2.png` (two frames, flowing motion) and summon assertion. Remaining: community UI wiring inside ClassDetail, rate limiting, real-hardware audio feel, a11y certification.
2026-09-25 — Community UI shipped end-to-end. `CommunityTab.jsx` (shared) renders the class community on both surfaces: the student's class view (`StudentClasses.jsx` gained a "Community" tab beside Classwork/Updates) and the teacher's ClassDetail (a "Community" tab beside Stream/Classwork/People/Insights). The composer enforces the 1–1000 character limit with a live counter and honest posting state; posts render chronologically with author name, a Teacher/Learner badge and timestamp; the assigned teacher sees "Remove" per post and removals disappear for everyone; failures surface as inline alerts; the empty state invites the first post; a footer line states the visibility boundary ("Visible to everyone in this class… Nothing here is public"). E2E verified in the production bundle (`scripts-tmp/community-e2e.mjs`): the enrolled learner posts, the assigned teacher sees and removes the post, removal propagates to the learner, 0 page errors. Two e2e fixture defects were found and fixed during verification (the teacher context lacked its workspace data entry, and the seeded classroom needed teacher ownership) — both were test-fixture issues, not product defects; the product behaved correctly throughout. Checks: 130/130 Node tests, typecheck, clean lint, production build.

— Roadmap agreed with the founder for completing the AGI product: (1) finish the remaining internal function work (community polish, plan interactions), (2) complete the internal backend part — the transport boundary, adapters and contracts are in place from the team's Gate 1–5; what remains server-side is real endpoints, authorization and data ownership verification feeding those seams, (3) connect the product to that backend through the existing seams without rewrites, (4) launch. The product side is ready to accept real content, model and account services.
2026-09-25 — Full five-category product audit (pre-backend gate, Google-education-engineer review). Rendered audit of the running product for every category — Student (Aarav), Teacher (Dev), Parent (Anika), Professional (Sam), Organization (School admin) — covering each role's Home, its core working pages (student: Learn/Practice/Build/Classes; teacher: Prepare/Classes/Learners; parent: Child/Reports; professional: Career/Build; organization: Cohorts/People/Analytics) and the Ask conversation for all five: **24 page checks, every one rendered with real content, 0 page errors, 0 console errors, 0 horizontal overflow at 1440 and 390** (script `scripts-tmp/role-audit.mjs`, captures `audit-*.png`). The AGI product is functionally whole across all categories: every role opens a working workspace with role-correct actions, the student loop and Daily Mentor Engine, teacher preparation/evidence/moderation, parent consent-scoped reports, professional career loop, organization aggregate admin, the always-on AGI orb with summon, and the class community.

Launch assessment (what remains between this product and market): (1) **the backend is the only blocker** — real accounts/sync, official curriculum through ContentRepository, real model through TeachingInterface/mentor adapter, server authorization and push notifications; the product side is seam-ready with no rewrites needed (team Gate 1–5 transport boundary accepts the connection). (2) Small product items that can land pre-backend: plan-step deferral ("Not today"), community rate limiting and a report/flag action. (3) Pre-launch checklist to run with the backend: real-device microphone QA, full accessibility certification (screen reader/200% zoom), interface-localization completion beyond English, low-end-device performance pass, PWA/store packaging decision, and activation of the existing support/grievance and billing flows. Nothing found in the audit blocks design or function work; the product behaves as a production Google-style application in its local-preview scope.
2026-09-25 — AGI animation finalized to the founder's Copilot-aurora reference. The orb is now an **aurora panel**: a deep Visionary navy field (the product's own blues, never black) with a luminous cyan-blue light band flowing across it (three layered sine strokes with soft glow), a drifting white-blue focal glow that swells with the real microphone level while listening, and the reference's violet corner tint. The panel is a rounded rectangle (12px corners, 56×44) filling with light rather than an icon-on-disc, sitting below the navigation; hover lifts it with a light ring. The summon tap (greeting spoken + caption, no navigation), always-running animation, honest per-state brightness/speed, turn-on bloom, and reduced-motion stillness are unchanged. Verified in the production bundle: aurora renders with flowing motion (two-frame captures `aurora-panel.png`/`aurora-final.png`), summon returns the exact line, 0 page errors; 130/130 Node tests, typecheck, clean lint, production build.
2026-09-25 — AGI animation redesigned after a live design study of the competitors. Browsed and captured gemini.google.com (presence = ambient blue light behind a clean input, no object), chatgpt.com (dark minimal UI; voice mode is a single organic breathing blob — page itself sits behind Cloudflare verification) and x.ai (Grok: stark black/white restraint, almost no animation). Synthesis: every chatbot's light is cool and impersonal; our intelligence is a mentor, so ours is the **mentor's lamp** — a living flame of Visionary blue with a warm golden heart (the diya of knowledge), organic and always lit, one step beyond the chatbots' orbs. Implemented in `AudioPresence.jsx`: deep navy field, a breathing organic light form (outer aura / blue body / bright inner light on an elongated vertical axis), an independently flickering flame tip, a warm white-gold heart, and a quiet pool of light at the base; every radius and offset breathes on its own slow rhythm, the whole form grows with the real microphone level while listening and flickers faster while speaking. Panel is 56×48 rounded; summon tap (spoken + caption greeting, no navigation), turn-on bloom, honesty tiers and reduced-motion stillness unchanged. Verified in the production bundle: flame renders with its full form, summon returns the exact line, 0 page errors (`flame-final.png`); 130/130 Node tests, typecheck, clean lint, production build.
2026-09-25 — User-selectable AGI animations shipped (founder references: soft sky orb + glowing equalizer). The AGI's presence below the navigation is now chosen by the user by name in Personalization — an "AGI animation" dropdown with two styles: **Orb** — a soft sky sphere (white light drifting over Visionary blues, like the founder's reference) whose heart glows with the real microphone level; **Voice** — four glowing rounded bars on a luminous blue field (the reference equalizer), each bar dancing to its own frequency band from the live analyser while listening, cadence-driven while speaking, gently rippling when idle. The preference (`preferences.agiAnimation: 'orb'|'voice'`, default orb, legacy-normalized) also switches the element's shape (circular sphere vs rounded panel). Summon tap, bloom entrance, honesty tiers and reduced-motion stillness apply to both. A production-bundle verification caught a REAL product bug introduced with the orb style: the drifting-light loop shadowed its loop variable (`const cloud` inside `for (let cloud…)`), a temporal-dead-zone crash that silently stopped the orb's clouds and glow from painting every frame — the founder's dissatisfaction was justified. Fixed (renamed to `cloudGlow`); both animations now render fully with 0 page errors (frames `anim-orb-1/2.png`, `anim-voice-1/2.png`). Files: `AudioPresence.jsx`, `WorkspaceTools.jsx` (AGI animation dropdown), `domain/workspace.ts`, `workspaceService.ts`, `workspace.css`, `scripts-tmp/animation-select-verify.mjs`. Checks: 130/130 Node tests, typecheck, clean lint, production build. Note for the team: Playwright `addInitScript(fn)` serializes the function — closure variables do not survive; embed values in the script text.
2026-09-25 — AGI animations renamed and finalized: "Vision Boy" and "Vision Girl" (founder references, exact designs). The Personalization dropdown now offers the two named designs: **Vision Boy** — the soft sky sphere: a circular ball whose upper half holds drifting white clouds over a deep blue gradient (reference image 1), clouds brightening with the real microphone level while listening; **Vision Girl** — a deep navy rounded panel with a luminous blue halo and **four glowing white egg-shaped bars** resting in the reference's composition (bar three tallest), each dancing to its own frequency band while listening, cadence while speaking, resting exactly at the reference composition when idle. Preference values are now 'boy'/'girl' with legacy 'orb'/'voice' migrated automatically ('voice'→Vision Girl, 'orb'→Vision Boy); the element shape follows the design (circle vs panel). Summon tap, bloom entrance, honesty tiers and reduced-motion stillness unchanged. Files: `AudioPresence.jsx`, `WorkspaceTools.jsx`, `domain/workspace.ts`, `workspaceService.ts`, `workspace.css`. Checks: 130/130 Node tests, typecheck, clean lint, production build; rendered verification of both named designs with the persisted preference read back (`anim-boy-1.png`, `anim-girl-1.png`), summon intact, 0 page errors.
2026-09-25 — "Old animation showing" report investigated to root cause: a **dead test server**, not the product. A localStorage write-tracer (setItem wrapped in an init script) run against a verified-live preview proved: with Vision Girl seeded, a full page load performs ZERO workspace-db writes and the preference persists (`agiAnimation: 'girl'` after load; earlier traces that showed the field missing were hitting a stopped preview — the page never loaded). The named AGI animations, the dropdown write path, and persistence are all confirmed correct end-to-end on the current build. Verification hygiene recorded: every browser check must assert the page actually loaded (e.g., 'Welcome back' visible) before reading state, and the preview server must be started in the same command as the test.
2026-09-25 — Founder design round: Vision Girl default, transparent, product-blue rectangle; Vision Boy matched; both always animated. (1) **Vision Girl redesigned**: the dark navy panel is REMOVED — the element is now just the four glowing blue bars (gradient #8ab4f8→#3b78f6 with soft blue glow) rippling over the white page, resting at the reference composition (bar three tallest) and dancing to their own frequency bands while listening. (2) **Vision Girl is the default** AGI animation (`emptyData` + legacy normalize: anything not 'boy' → 'girl'). (3) The working-surface rectangle border is now **product-colored** (#aecbfa, the Google blue-tint) instead of gray. (4) **Vision Boy** rebuilt to match the reference sphere: white cloud light traveling across the top half (wrapping drift), blue gradient deepening toward the bottom, heart glow swelling with the real microphone level. (5) **Always animated**: the OS reduced-motion setting now slows the motion (0.25×) instead of freezing it — the animation is the on-signal, so it never disappears; verified frames differ across time in both designs. Files: `AudioPresence.jsx`, `workspaceService.ts`, `workspace.css`. Checks: 130/130 Node tests, typecheck, clean lint, production build; rendered verification of both named designs (`anim-boy-1.png`, `anim-girl-1.png`), summon intact, 0 page errors.
2026-09-25 — Founder round: reactive working-surface rectangle + Vision Girl/Vision Boy refinements. (1) **The main working-surface rectangle is now the AGI's voice visualizer**: its border is product-blue and reactive — `AudioPresence` publishes the live state every frame through the `--agi-ring` CSS variable (bright and level-driven while listening, softer while speaking, gentle when ready, quiet when audio is off), and `.workspace-surface` renders its border color and a soft outer glow from that variable (verified: ring 0.300 idle → 0.685 listening with "Listening." status; the border visibly brightens between captures `rect-idle.png` / `rect-listening.png`). (2) **Vision Girl**: the halo/background is removed entirely — the element is now only the four glowing blue bars on the white page — and the blue hover ring on the AGI button is removed (hover keeps only the gentle lift). (3) **Vision Boy** enriched toward the reference: a wide bright sky band across the top, four traveling cloud masses, and a soft depth vignette toward the bottom-right. Files: `AudioPresence.jsx`, `workspace.css`. Checks: 130/130 Node tests, typecheck, clean lint, production build; rendered verification of both designs + rectangle states, 0 page errors.
2026-09-25 — Founder design fix + backend phase opened. (1) The working-surface rectangle border changed to the product's black (#121317) and the glow shadow removed — the product does not use shadows; the reactive `--agi-ring` publishing was removed with it (dead code). The AGI's animation presence remains in the nav (Vision Boy / Vision Girl). (2) **Backend phase started** with `docs/visionary/BACKEND_CONTRACT.md`: the complete server-side build spec derived from the verified client boundary (backendTransport.ts, Gate 1) — session/authorization rules per role, the four live operations (content.syllabus, content.concept, teaching.request, mentor.turn) with payloads and non-negotiables, plus the five launch operations specified at contract level (auth.session, data.sync, classroom.entity, community.post, notifications.push) to be wired into the transport after founder decisions. Founder questions recorded for the backend discussion: backend stack/hosting and who builds it; model provider and key ownership; official curriculum source and languages; account policy (email only vs Google sign-in, phone verification); guardian-verification approach for minors (DPDP); launch domain and PWA/store packaging.
2026-09-25 — Founder border refinement: the working-surface rectangle now uses Google's standard light border (#dadce0) and a larger 24px corner radius (was #121317/16px). Verified in the production bundle (border-final.png); 130/130 tests, lint clean, build passes.
