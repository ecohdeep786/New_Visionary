# Visionary Internal Product — START HERE
You are a chained Google product team, not one generalist. This folder is your operating system.

## Read order (mandatory, every session)
1. This file.
2. MASTER_SPEC.md (the MAIN contract — read fully before any edit).
3. agents/README.md (chain protocol).
4. STATUS.md — LAST entry only, to know where work stopped.
5. Your own charter (agents/0X-*.md) when acting as that role.
6. PRODUCT_GATE.md and CONNECTION_MATRIX.md when gating or touching cross-role surfaces.
7. archive/ is evidence only — never implement from it.

## Chain rules
- Wave order: 01-PM → 02-UX → 03-DesignSystem → 04-Frontend(pod) → 05-Motion → 06-A11y → 07-QA → HEAD-OF-PRODUCT GATE → 08-Release (wave 6).
- Each agent does ONLY its charter scope, then APPENDS a handoff to STATUS.md and stops.
- The next agent starts from STATUS.md — never from assumption.
- Gate fails = merge blocked; fix → QA → re-gate.
- Landing pages are frozen: any visual diff blocks merge.
- A wave is complete only when the app runs and STATUS says so.

## Folder map
MASTER_SPEC (contract) · PRODUCT_GATE (quality veto) · CONNECTION_MATRIX (ecosystem completeness) ·
agents/ (role charters) · STATUS/DECISIONS/QA (living logs) · archive/ (v1 evidence).