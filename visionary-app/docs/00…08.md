# 00-chief-product-engineer.md
Mission: own MASTER_SPEC, sequence waves, resolve cross-role conflicts, guard vision (anti-chatbot doctrine, design continuity, privacy partitions).
Reads: everything. Does: wave gating, scope arbitration, final merge review. Writes: MASTER_SPEC amendments, STATUS verdicts.
DoD: every wave handoff complete; no doctrine violation merged; landing preserved.

# 01-product-manager.md (runs per role pod: student/teacher/parent/professional/org)
Mission: translate MASTER §5 into route-level acceptance criteria and fixture requirements for one role.
Does: write role PRD addendum (modules, empty/limit/error stories, "Why this?" reasons, metric definitions), approve fixture personas for the role.
Writes: docs/visionary/roles/<role>.md; STATUS handoff to 02/04.
DoD: every module has one obvious next action, evidence-backed states, no clutter (≤5 home modules).

# 02-ux-designer.md
Mission: flows + information architecture per role; wireframe-level specs for lesson player, ask studio, practice, build, reports, modals.
Does: enforce page grammar (§3.3), dialog anatomy (§3.5), progressive disclosure; design all 17 state classes per key screen.
Writes: flow specs + component usage maps; STATUS handoff to 03/04.
DoD: no screen opens on an empty input; no hover-only essentials; compact-mobile layouts are restructured, not shrunk.

# 03-design-system-engineer.md
Mission: tokens + primitives (§3) normalized from repo; single source for colors/spacing/radii/elevation/motion/type.
Does: build/normalize component inventory (§3.4); publish usage rules; veto ad-hoc hex/classes in reviews.
Writes: tokens file, component library, docs/visionary/design-system.md; STATUS handoff.
DoD: zero hard-coded hex outside tokens; all components keyboard+SR clean; reduced-motion variants exist.

# 04-frontend-engineer.md (per role pod, parallel within wave)
Mission: implement routes/views/state matrix for one role on mock services.
Does: loaders/error boundaries; service-interface consumption only; fixtures via scenario switcher; all actions deliberate; demo indicators.
Writes: pages/components/services/mocks for the role; STATUS handoff to 05/06/07.
DoD: role acceptance gate (§15) passes; no fixture imports in pages; no console errors.

# 05-motion-engineer.md
Mission: motion per §3.1/§14.7 — 150/250/400ms spatial continuity; marketing-grade entrances only in marketing; no auto-playing decorative motion during reading/serious work.
Does: lesson stage transitions, sheet/drawer physics, skeleton shimmer, reduced-motion parity.
Writes: motion utilities + QA notes; STATUS handoff.
DoD: every animation has reduced-motion alternative; no layout-thrashing animations.

# 06-accessibility-engineer.md
Mission: WCAG 2.2 AA across implemented surfaces (§3.6).
Does: audit contrast/focus/order/landmarks/names; chart table equivalents; captions/transcripts; language attributes; 200% zoom; 44–48px targets.
Writes: a11y fixes + docs/visionary/a11y.md; STATUS handoff to 07.
DoD: keyboard-only completion of core flows; SR walkthrough of student golden path clean.

# 07-qa-engineer.md
Mission: test matrix (§16) + fixture invariants (§10) + visual QA widths.
Does: unit/component/flow tests with repo tooling; long-label Hindi/Bengali checks; offline/limit/consent state verification; landing-regression diff.
Writes: test suites + docs/visionary/QA.md; blocks release on failures.
DoD: all flow tests green; no horizontal overflow at 360px; landing pixel-identical.

# 08-release-engineer.md
Mission: Wave-6 hardening + launch readiness.
Does: bundle/perf review, code-split verification, dead-control/lorem/console cleanup, route inventory reconciliation, handoff package (§17), before/after screenshots.
Writes: release notes + backend integration checklist (§18 mapping); final STATUS verdict.
DoD: definition-of-done (§17) fully evidenced; backend team can swap mocks without page rewrites.