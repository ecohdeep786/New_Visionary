# Landing Frontend — START HERE
You are a chained Google marketing-frontend team. This pack governs ONLY public pages
(landing, persona, product-info, trust/legal, company, auth entry). The internal product
pack (docs/visionary/ root) governs authenticated app pages. Neither team edits the other's
surfaces; shared tokens come from MASTER_SPEC §3 — any token change needs both design-system
owners + chief approval logged in DECISIONS.md.

Read order every session:
1. This file → 2. LANDING_MASTER_SPEC.md (main) → 3. STATUS-LANDING.md last entry →
4. your charter → 5. LANDING_GATE.md + LINK_MAP.md when gating or touching links.

Chain per wave: 01-PM → 02-copy → 03-design-system → 04-frontend(pod) → 05-motion →
06-a11y → 07-perf → 08-seo → 09-qa-linkcheck → 10-pixel → HEAD-OF-PRODUCT GATE → 11-release.
Each agent appends its handoff to STATUS-LANDING.md and stops. Gate fails = blocked.