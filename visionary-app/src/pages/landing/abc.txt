

You are building a AGI (Artifical General Super Intelligence) Product product for learning and building.
SUBJECT: Build the internal Visionary product now. Do not turn this into a documentation project.

IMPORTANT INTENT
Previous agents produced a lot of useful STATUS, DESIGN_INVENTORY, UX, QA and DECISION documents. That phase is enough for now. Your job is NOT to write more analysis, NOT to create new design docs, NOT to redesign marketing pages, and NOT to simulate model answers locally.

Your job is to IMPLEMENT the internal product so the backend/frontend flows are complete enough for beta, with real seams for ML and database content to be connected later.

Read this instruction fully, then execute in order. Do not stop to ask for review unless you encounter an irreversible, security, data-loss or destructive-schema decision. Continue autonomously.

═══════════════════════════════════════════════════════════
0. CONTEXT YOU MAY USE
═══════════════════════════════════════════════════════════
Visionary is India's Pedagogical AGI for learning and building in native languages.
The product serves five categories through one shared intelligence:
  - Student
  - Teacher
  - Parent
  - Professional
  - Organization

The four permanent product apps are:
  LEARN · ASK · PRACTICE · BUILD

The core architecture is VATS:
  L1 Perception
  L2 Student Cognition Model / SCM
  L3 Adaptive Teaching Engine / ATE
  L4 Model Stack / inference seam
  L5 3D visualization seam
  L6 Memory
  L7 Data flywheel / interaction event logging

The current internal repo already has:
  - docs/visionary STATUS, DECISIONS, QA, DESIGN_INVENTORY
  - role addenda under docs/visionary/roles
  - existing dashboard routes and Home/Ask implementation
  - existing tests and Vite app
  - existing blue/white shell and workspace CSS

Do not restart the repo. Do not rewrite working code unless it is blocking. Preserve existing deep links, routes, session storage, classwork, artifacts, tests and user-visible behavior unless a bug requires correction.

═══════════════════════════════════════════════════════════
1. HARD RULES — DO NOT VIOLATE
═══════════════════════════════════════════════════════════
1. CODE FIRST, DOCS AFTER.
   Do not create a new master plan, UX doc, inventory, architecture essay, or status document before shipping code.
   After each completed module, write only a short append-only STATUS/QA entry: one paragraph, files touched, tests run, remaining gap.

2. NO MODEL-ANSWER SIMULATION.
   Do not build local fake LLMs, fake pedagogy generators, fake answer-quality harnesses, or simulated "model answers" to test content.
   Real answers will come later from ML + database. Your job is to create the connection seam, not fake the intelligence.

3. NO CURRICULUM HARDCODING.
   Subject, chapter, topic, concept and course data must come from a ContentRepository interface backed later by the 7-level syllabus database:
   Board → Class → Subject → Textbook → Chapter → Topic → Concept.
   You may include a tiny seed/sample dataset for local development, but production code must not depend on hardcoded curriculum.

4. NO PER-ROLE PARALLEL PRODUCTS.
   Student, Teacher, Parent, Professional and Organization are adapters/views over the same SCM + app shell.
   Do not build five separate apps. Build one product with role-based data views and permissions.

5. NO MARKETING PAGE WORK.
   Do not touch Landing, Student, Teacher, Parent, Professional, Organization marketing pages.
   Do not implement the 25 supporting website pages.
   This task is internal product only.

6. NO VISUAL REBRAND DETOURS.
   Preserve the current shell and design DNA unless the founder explicitly orders a teal/blue token swap.
   If colors need abstraction, put them behind existing token/CSS variables. Do not spend implementation time redesigning.

7. NO FALSE CAPABILITY CLAIMS.
   The product may use stubs for ML/DB, but UI copy and logs must not claim live AGI, live model analysis, voice listening, sync, or real curriculum coverage when those are not connected.
   Use honest labels like "demo", "provisional", "not connected", or "saved locally" where required.

8. NO DELETION OF USER/TEAM WORK.
   Existing unrelated working-tree changes belong to the user/other team. Do not revert them.
   Do not delete demo fixtures globally. Only retire identified fixtures when real backend connection exists.

═══════════════════════════════════════════════════════════
2. PRIMARY MISSION
═══════════════════════════════════════════════════════════
Complete the internal backend/product surface for all five categories so the product is beta-ready, with clean seams for ML and database integration later.

The most important launch path is the Student loop:
  Home → Learn → Ask → Practice → Build → SCM update → L7 event → memory/moat card.

After the Student loop is real and stable, implement the other categories as role adapters over the same services:
  Teacher: class aggregate, quiz builder, lesson plan builder, teacher coach seam.
  Parent: read-only view of one child's SCM + weekly report seam.
  Professional: own SCM with career target, skill gap, build/project seam.
  Organization: aggregate-only analytics, admin/setup, cohort/curriculum/library seams.

Do not aim for perfect AI answers. Aim for a complete product pipeline that can accept real ML/DB answers later without rewriting.

═══════════════════════════════════════════════════════════
3. EXECUTION ORDER
═══════════════════════════════════════════════════════════
Follow this order. At each step, implement, test, then move on.

STEP A — QUICK REPO HEALTH CHECK
- Run existing tests, lint, typecheck and production build.
- Fix only blockers that prevent implementation.
- Do not refactor broadly.
- Output a 5-line maximum plan, then start coding.

STEP B — DEFINE THE REAL SERVICE SEAMS
Create or complete these interfaces/services with clear contracts:
  1. ContentRepository
     - getSyllabus(board, class, subject)
     - getChapters(...)
     - getTopics(...)
     - getConcepts(...)
     - getConcept(conceptId)
     - provisional fallback when DB miss
  2. SCMService
     - getStudentState(studentId)
     - updateMastery(event)
     - getWeakConcepts(studentId, threshold)
     - getReadyConcepts(studentId)
     - getPrerequisitePath(conceptId)
  3. TeachingInterface
     - requestExplanation(promptPacket)
     - requestPracticeQuestion(promptPacket)
     - requestFeedback(promptPacket)
     - requestProjectGuidance(promptPacket)
     This must be a thin interface. It may point to a deterministic local stub for pipeline testing, but the stub must be obviously labeled as stub and must not be used to evaluate answer quality.
  4. MemoryService
     - saveEpisodicEvent(...)
     - getRecentContext(userId)
     - getWeeklyObservations(userId)
  5. EventEmitter / L7 flywheel
     - emitInteractionEvent(event)
     - event schema must include user_id, role, session_id, app, concept_id, input_type, language, board/class/exam if known, pedagogy used, verification result, latency, timestamp, prompt_version if available.
  6. AccessControlService
     - student: own data only
     - teacher: assigned class/cohort aggregate only
     - parent: connected child only, consent-scoped
     - professional: own data only
     - organization: aggregate-only, no individual private data unless explicitly permitted

STEP C — MAKE THE STUDENT LOOP END-TO-END REAL
Implement the student product flow using the services above.

1. Student Home
   - Show one next action based on saved activity, due practice, weak concept, or starting point.
   - Do not invent weakness if no evidence exists.
   - Show "Things I remember about you" card using rule-generated weekly observations from SCM/events, not live ML.
   - Example safe observation:
     "This week you revisited Fractions 3 times and your accuracy improved."
     "You have not practiced Newton's Laws since Sept 12."
   - This moat card is launch-critical. Implement it now.

2. Learn
   - Chapter → topic → concept flow from ContentRepository.
   - If syllabus is missing, use provisional skeleton and conversational fallback, but do not block the user.
   - Teaching request goes through TeachingInterface.
   - After explanation, run comprehension check.
   - Comprehension result updates SCM.
   - Every view/interaction emits L7 event.

3. Ask
   - Preserve existing Guide/conversation behavior.
   - Intent/context selection must map to TeachingInterface mode.
   - Do not auto-send.
   - Drafts, session, history and activity resume must survive refresh.
   - Every Ask turn emits L7 event with intent, language, concept if known, and response status.

4. Practice
   - Adaptive quiz from SCM weak/ready concepts.
   - Correct answer raises difficulty; wrong answer lowers/remediates.
   - Wrong answer triggers re-explanation through TeachingInterface, not hardcoded answer text.
   - Practice result updates SCM.
   - Every question/answer emits L7 event.

5. Build
   - Project/artifact creation tied to mastered concepts.
   - Artifact saved to user workspace.
   - Build completion emits strongest mastery signal to SCM.
   - Every build action emits L7 event.

STEP D — IMPLEMENT THE MINOR-CASE / MISSING SYLLABUS FLOW
This is required because real DB content will arrive later.

When ContentRepository cannot find official syllabus for board/class/subject:
  - Do not crash.
  - Do not show empty product.
  - Create provisional numbered chapters.
  - Allow user to add subject/chapter names optionally or skip.
  - Flag data gap internally:
      board, class, subject, user_id, timestamp.
  - Product continues in conversational/provisional mode.
  - When official data later arrives, map provisional → official without losing user progress.

STEP E — IMPLEMENT OTHER ROLE ADAPTERS
Do not build full polished role products yet. Build the minimum real adapters so the product architecture is complete and not student-only.

1. Teacher
   - Teacher has own SCM too.
   - Teacher Home shows:
       continue preparation, pending submissions, class attention items, own growth.
   - Class aggregate view:
       read-only heatmap from student SCM events for assigned class.
   - Quiz Builder:
       generate practice set targeting weak class concepts via TeachingInterface seam.
   - Lesson Plan Builder:
       scaffold lesson plan via TeachingInterface seam.
   - No private student doubt exposure beyond permitted class evidence.

2. Parent
   - Parent Home shows one connected child's weekly summary.
   - Read-only view over child SCM/events.
   - Consent/pending/revoked states must be real.
   - No access to other children.
   - No raw private conversation logs.
   - Weekly report generated from rule-based observations, not live ML.

3. Professional
   - Professional uses same student loop but with career target as exam_target/goal.
   - Home shows skill gap, next capability, build project.
   - Build/portfolio is emphasized.
   - No school/board terminology in personal career context unless the user's goal requires it.

4. Organization
   - Organization Home shows setup checklist and scoped operational attention.
   - Analytics are aggregate-only.
   - People/cohorts/curriculum/library are admin surfaces over the same services.
   - Do not expose individual learner private data.

STEP F — SAFETY, PRIVACY AND CHILD PROTECTION
Implement these as code-level guards, not documents.

- Role/age eligibility must prevent minor users from receiving professional/adult journey fallbacks.
- Crisis keywords on L1/input path must hard-stop normal teaching and return a safe response with helpline placeholder/config.
- PII must not be emitted into L7 flywheel events.
- DPDP-style controls:
    user can view memory,
    user can delete memory,
    consent state controls parent/teacher/org access.
- Access control must be enforced in services, not only UI.

STEP G — TESTS THAT MATTER
Write tests for your own code, not for fake model answer quality.

Required test coverage:
  - ContentRepository provisional fallback on missing syllabus.
  - SCM update after Learn comprehension check.
  - SCM update after Practice correct/wrong.
  - Build completion emits mastery signal.
  - L7 event emitted for each app interaction.
  - PII scrubbing in events.
  - Role/age eligibility blocks minor from professional journey fallback.
  - Parent access only to connected child.
  - Teacher access only to assigned class aggregate.
  - Organization access aggregate-only.
  - Existing Home/Ask regressions remain passing.
  - Production build passes.

Do not add tests that assert the quality of generated teaching answers. That is ML evaluation work after real model connection.

═══════════════════════════════════════════════════════════
4. WHAT TO DO WITH EXISTING HOME/ASK WORK
═══════════════════════════════════════════════════════════
The current STATUS says Home/Ask is mostly implemented and under corrective QA.

Do not restart it.
Do not redesign it.
Do not write new UX docs for it.

Only do this:
  - Run existing tests.
  - Fix only blocking bugs in Home/Ask if they prevent the student loop.
  - Preserve 04/06 semantics and 03 scoped styling.
  - If M-01 shared smooth-scroll defect is still blocking accessibility, fix it as a one-line scoped/shared-style correction with public-preservation check, then move on.
  - After that, focus on Learn/Practice/Build/SCM/L7.

═══════════════════════════════════════════════════════════
5. DOCUMENTATION RULE
═══════════════════════════════════════════════════════════
Documentation is allowed only after code ships.

For each completed module, append one short STATUS entry:
  - module name
  - files touched
  - tests run
  - result
  - remaining gap
  - next module

Do not create:
  - new master spec
  - new design inventory
  - new UX flow doc
  - new architecture essay
  - new product gate doc
  - new long QA matrix

If you find yourself writing a document before code, stop and implement instead.

═══════════════════════════════════════════════════════════
6. DEFINITION OF DONE FOR THIS PHASE
═══════════════════════════════════════════════════════════
This phase is done when all of the following are true:

1. A student user can complete:
     Home → Learn → comprehension check → Ask → Practice → Build
   and the system persists:
     SCM state,
     session/conversation state,
     artifact/project state,
     L7 interaction events.

2. Missing syllabus does not block the user.
   Provisional mode works and data-gap flag is emitted.

3. Teacher, Parent, Professional and Organization can open their role Home without crashing and see honest role-appropriate data/views over the same services.

4. Access control is enforced:
     student own data,
     teacher class aggregate,
     parent connected child only,
     professional own data,
     organization aggregate only.

5. L7 flywheel emits structured events for every meaningful interaction in the student loop.

6. The "Things I remember about you" moat card exists on Student Home using rule-generated observations from real events/SCM.

7. Tests pass for service seams, SCM updates, L7 events, access control, minor eligibility, provisional syllabus fallback, and production build.

8. No new documentation project was created ahead of code.

═══════════════════════════════════════════════════════════


If you need a decision, ask only when the decision is irreversible, security-sensitive, destructive, or would change founder-visible product policy. Otherwise choose the safest reversible implementation and continue.

BEGIN NOW.

Note: Why i am giving this prompt to you because the AGI we are building that fetch the data from database and model response from backend later so current we have build a complete part only backend connect need later and all will work, everything is worked current but the product AGI will fetch the subject of user from data base and learning and building happend start from there of each subject of student(if i giving one example) and everything will go depend on student subject, and extra learning other from subject, building happend same in product and upgrading the connect class, learning same from connect class and everything show depend on the data,
Keep in mind: We achieve AGI by building a Daily Mentor Engine: a system that combines a Student Cognition Model, curriculum/class graph, daily planning engine, teaching loop, memory layer, and data flywheel. The product does not wait for the student to ask. It knows the student, plans the day, teaches until understanding is verified, and guides the learner from classwork to homework to projects to class progression. The student never has to decide what to learn or how to learn it. Visionary removes decision fatigue and makes learning feel guided, personal, and continuous — like a real mentor holding the student’s hand every day.

 When a student opens a subject, Visionary shows the subject progress and the chapters inside it. The AGI already knows what the student is learning, what they have mastered, what they are struggling with, and what should come next.
When the student opens a chapter, Visionary does not simply show a static chapter. It converts the chapter into a guided learning path: prerequisite concepts, micro-lessons, visual explanations, 3D scenes where useful, comprehension checks, practice questions, and a final build activity.
The student can start the chapter, continue where they left off, ask doubts naturally, practise adaptively, and build something real from what they learned. The AGI teaches like a human mentor — step by step, in the student’s language, at the student’s level — and never moves forward until understanding is verified.
This same intelligence serves students, teachers, parents, professionals and organizations, but each role sees the journey from its own perspective.
Subject
 └── Chapter
      └── Concepts
           └── Learning Units
                ├── Explain
                ├── Visual / 3D / Diagram / Simulation
                ├── Check understanding
                ├── Ask doubt
                ├── Practice
                └── Build / Apply

i gave the example of student and you have to make for all category, now work serious on product.